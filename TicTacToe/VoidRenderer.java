/**
 * A Void Renderer class. This class implements a void renderer, meaning it doesn't show
 * the board when it's used.
 */
public class VoidRenderer implements Renderer {

    /**
     * The method does nothing. The board will not be shown during the game.
     * @param board the game's board
     */
    public void renderBoard(Board board) { }
}
